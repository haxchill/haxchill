name: Deploy HaxBall Bot

on:
  push:
    branches: [ main, dev ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 🧪 Install dependencies
        run: npm install

      # - name: 🧪 Run tests
      #   run: npm test

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            set -e
            cd /opt/haxball-bot || { git clone https://github.com/${{ github.repository }} /opt/haxball-bot && cd /opt/haxball-bot; }
            git fetch origin ${{ github.ref_name }}
            git reset --hard origin/${{ github.ref_name }}
            cat > .env << 'EOF'
            ROOM_NAME=${{ secrets.ROOM_NAME }}
            ROOM_MAX=${{ secrets.ROOM_MAX }}
            ROOM_PUBLIC=${{ secrets.ROOM_PUBLIC }}
            ROOM_PASSWORD=${{ secrets.ROOM_PASSWORD }}
            ROOM_GEO=${{ secrets.ROOM_GEO }}
            HAXBALL_TOKEN=${{ secrets.HAXBALL_TOKEN }}
            EOF
            npm install
            pm2 reload haxball-bot || pm2 start index.js --name "haxball-bot"